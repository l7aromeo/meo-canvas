# Pinned so a local build and CI apply the same clippy lints. `just fmt` uses
# the pinned nightly by name for rustfmt's unstable options, which keeps the
# build itself on stable.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy", "llvm-tools-preview"]
profile = "minimal"
