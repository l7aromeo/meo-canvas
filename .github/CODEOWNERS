# Review is requested from a path's owner when a pull request touches it. One
# owner, so the pattern is broad: what matters is that nothing merges unreviewed
# under branch protection. If ownership splits, the seams are `crates/`,
# `packages/` and `.github/workflows`.

* @l7aromeo
