# The sponsor button GitHub renders on the repository and every release page.
# `github` needs the account enrolled at github.com/sponsors, and until then the
# entry is inert rather than broken.
github: l7aromeo

# One-off tipping, and it renders immediately -- unlike `github` above, which
# stays inert until the account is enrolled at github.com/sponsors.
ko_fi: l7aromeo
